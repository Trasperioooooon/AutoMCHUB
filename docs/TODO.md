# AutoMCHUB 体验改进 TODO（全量实施清单）

> 依据：`docs/UI与体验改进调研报告.md`（2026-07-12）。用户决策：**全部改动一起做、做到最好、不计时间**。
> 目标版本：v2.1.0。本清单为唯一施工依据，每项完成即勾选；「验收」列不通过不算完成。
> 横切纪律（每个阶段收尾必做）：`go vet` + 构建通过；UI 改动出**双主题截图并阅图确认**；行为改动做真机 E2E；按阶段提交 commit（延续中文提交信息风格）。

## 总览

| 阶段 | 主题 | 任务数 | 依赖 |
|---|---|---|---|
| 0 | 基建（供后续复用） | 4 | — |
| 1 | Bug 速修 | 3 | 0.1 |
| 2 | 暖沙浅色主题 | 3 | — |
| 3 | 设置页信息架构 | 6 | 0.3 |
| 4 | server.properties 全量目录 | 6 | 0.2 / 0.3 / 0.4 |
| 5 | 存储位置（三层） | 7 | 3（存储分区落位） |
| 6 | 创建向导升级 | 5 | 5.1（路径显示） |
| 7 | 实例列表与详情 | 7 | 0.1 |
| 8 | 联机页打磨 | 2 | — |
| 9 | 体验打磨 | 5 | 3 |
| 10 | 托盘与自启 | 2 | — |
| 11 | 收尾与回归 | 6 | 全部 |

---

## 阶段 0 · 基建

- [ ] **0.1 `/api/app` 扩展**：返回 `port`（实际监听端口）与 `availRamMb`（`GlobalMemoryStatusEx.AvailPhys`，app.go 加 `AvailRAMMB()`）。
  验收：改用 `-port 0` 类随机端口启动，接口返回真实端口；availRamMb 与任务管理器可用内存一致（±5%）。
- [ ] **0.2 前端版本比较工具 `verGte(mc, ref)`**：支持 `1.x.y` 与年份版号 `26.x` 混排（26.x 恒大于 1.x），供配置目录版本感知使用。
  验收：单测样例 `1.12.2<1.18`、`1.21.9≥1.19`、`26.1>1.21.9`、`26.2≥26.2` 全对。
- [ ] **0.3 行式控件组件（CSS + 渲染函数）**：统一「标题 + 副标题 + 右侧控件」行模式；含可展开行（收起显示当前值摘要）与行内嵌编辑器三种形态（HMCL 三件套）。
  验收：同一渲染函数支撑设置页与常用设置两处，双主题截图行高/对齐一致。
- [ ] **0.4 `PROPS_CATALOG` 数据文件**：按报告附录 B 录入约 60 项 × 7 组（键/类型/枚举值+中文名/默认值/中文名称/说明/引入版本/移除版本/是否需重启/gamerule 迁移目标）。level-type 按版本给三套枚举（1.12.2 大写 / 1.16–1.18 驼峰 / 1.19+ 命名空间 ID）。
  验收：脚本比对目录键名与附录 B 表格零遗漏；每项均有中文说明。

## 阶段 1 · Bug 速修

- [ ] **1.1 局域网指引端口修复**：设置页访问地址与防火墙命令改用 `location.port`（兜底 `/api/app` 的 port），消灭硬编码 27333。
  验收：`-port 28000` 启动，设置页显示的地址与 netsh 命令均为 28000。
- [ ] **1.2 放开 16GB 内存上限**：app.js 三处（向导/导入/JVM 页）`min(16384, ram-2048)` → `ram-2048`；>16G 时提示「过大的堆可能增加 GC 停顿」。
  验收：本机（按实际内存）滑条可拉到 `总内存-2G`。
- [ ] **1.3 内存滑条升级为「滑条 + 精确输入框 + 实时状态条」**（HMCL 式）：状态条显示「已使用 X GB / 总内存 Y GB ｜ 分配 Z GB」，分配 > 可用时红字警示「设备当前仅 X GB 可用」。三处（向导/导入/JVM）统一组件。
  验收：拖动滑条数字框联动、直接输入数字滑条联动；开大内存程序后状态条数值下降、超配警示出现。

## 阶段 2 · 暖沙浅色主题

- [ ] **2.1 替换浅色 token 为「暖沙中性」**（报告附录 A 方案 B）：bg `#efece2` / surface `#fbfaf5` / surface-2 `#f5f3ea` / sunken `#e6e2d6` / border `rgba(62,55,32,.13/.26)` / text `#221f18` / muted `#625d4e`；绿、红强调色不动。
  验收：附录 A 对比度表逐项复测达标（text/surface ≥15、muted/surface ≥6）。
- [ ] **2.2 `renderMotd` 浅色适配复验**：基色 `#565e57` 与 ×0.55 压暗系数在暖底上重校（必要时微调），16 色样例逐色确认可读。
  验收：含全部 §0-f 色码的 MOTD 在浅色列表页截图上逐色可辨。
- [ ] **2.3 双主题全页截图验收**：实例列表/向导四步/详情全部标签/联机/设置，浅色深色各一轮，阅图确认无踩色、无发丝边框消失。
  验收：截图存档，肉眼过全部页面。

## 阶段 3 · 设置页信息架构

- [ ] **3.1 页内二级导航骨架**：左侧子导航（200px 级）+ 右侧内容区，子路由 `#/settings/{section}`，刷新/直链可定位到分区。
  验收：六个分区可点击切换、当前项高亮、直链进入正确分区。
- [ ] **3.2 六分区迁移**：下载与网络（下载源三选 + CF Key）/ 存储位置（占位，5.2 落位）/ Java / 远程访问 / 通知与集成（Webhook）/ 更新与关于。全部改为 0.3 行式控件。
  验收：原设置页每一个功能点在新结构中可用且行为不变（逐项过一遍保存/生效）。
- [ ] **3.3 「更新与关于」页**：版本号、检查更新、更新仓库（默认值固化进程序，设置仅覆盖）、「启动时静默检查更新」开关、开源许可、「基于 OpenFrp OPENAPI」署名升为正式条目、GitHub 链接、退出程序（红色危险区独立分隔）。
  验收：静默检查开关持久化并在下次启动生效（日志可见）；署名可见性满足 OpenFrp 接入条款。
- [ ] **3.4 主题三态**：浅色 / 深色 / 跟随系统（HUD 按钮循环三态或设置内选择，`amh_theme=auto` 时监听 `prefers-color-scheme` 变化实时切换）。
  验收：切系统深浅色，auto 模式下界面实时跟随；显式选择后不再跟随。
- [ ] **3.5 Java 分区行式化**：便携/扫描列表改为行式（版本标题 + 路径副标题 + 右侧操作），扫描/手动添加保留。
  验收：与 3.2 同标准。
- [ ] **3.6 远程访问分区完善**：访问地址二维码（纯前端生成，报告路线图承诺项）、密码修改、防火墙命令（端口来自 1.1）。
  验收：手机扫码可直达登录页。

## 阶段 4 · server.properties 全量目录

- [ ] **4.1 「常用设置」页重构**：由 `PROPS_CATALOG` 驱动，7 组分组呈现 + 页首分组 chips 锚点 + 搜索框（中文名/键名过滤）+ 0.3 行式控件；数字项带范围校验；「需重启」项标注。
  验收：约 60 项全部可见可改可存；搜索「视距」「rcon」均能定位。
- [ ] **4.2 版本感知显隐**：按实例 MC 版本（0.2 verGte）隐藏不适用项——1.17- 隐 simulation-distance、1.21.2+ 隐 spawn-animals/npcs、1.21.9+/26.x 隐已迁移四项等，避免写死键。
  验收：1.12.2、1.20.1、26.x 三个实例的常用设置页各自显隐正确。
- [ ] **4.3 gamerule 模式**：1.21.9+/26.x 上 pvp/生成怪物/命令方块/允许下界转为 gamerule 控件——运行中通过控制台发 `/gamerule` 即时生效，停机时置灰提示「启动后可改」；注意 enableCommandBlocks 默认翻转为 true 的说明文案。
  验收：26.x 实例运行中切换 pvp，游戏内 `/gamerule pvp` 查询值同步。
- [ ] **4.4 「全部配置」补中文名**：raw 表对目录内已知键加中文名列，未知键（Paper 等自有配置）保持原样。
  验收：已知键显示中文名，陌生键不受影响。
- [ ] **4.5 创建流水线联动 enforce-secure-profile**：`install.go` 步骤 4，onlineMode=false 时写 `enforce-secure-profile=false`（仅 1.19+ 实例写入）。
  验收：新建离线 1.20.1 实例的 server.properties 含该键=false；1.12.2 实例不写。
- [ ] **4.6 白名单默认值预警**：26.3+ 实例创建完成页与常用设置中提示「此版本起白名单默认开启」；创建流水线按用户离线/正版选择给出合理初始值。
  验收：26.3 快照实例可复现提示（无 26.3 时以目录数据驱动的提示逻辑单测代替）。

## 阶段 5 · 存储位置

- [ ] **5.1 T1 路径透明化**：向导第 4 步与导入页实时显示目标路径（跟随实例名变化）；任务完成页、详情页头部显示路径。
  验收：改名时路径同步变化；三处路径与实际创建位置一致。
- [ ] **5.2 T2 全局「服务器存放目录」「备份目录」可配**：config.json 新增 `serversDir`/`backupsDir`（空=默认）+ 曾用根目录列表 `usedRoots`；设置「存储位置」分区落位（当前值摘要 + 浏览按钮 + 各目录「打开」）。变更仅对新建生效。
  验收：改到 D 盘后新建实例落 D 盘；旧实例原地可用。
- [ ] **5.3 Manager 多根扫描**：启动时扫描 默认目录 + 配置目录 + usedRoots + 注册的自定义实例目录；同名冲突时后者跳过并日志告警。
  验收：三个根各放一个实例，列表全部出现且可启停。
- [ ] **5.4 文件夹选择器**：Win32 `IFileOpenDialog`（`FOS_PICKFOLDERS`）纯 Go COM 封装，`POST /api/pickdir`；PowerShell `-STA` FolderBrowserDialog 兜底；**仅 localhost token 会话可调用**（LAN 会话返回 403 及提示）。
  验收：点浏览弹系统对话框、取消不报错、选择后路径回填；手机端点浏览得到明确提示而非无响应。
- [ ] **5.5 T3 每实例自定义路径**：向导「高级：自定义存放位置」折叠项（默认收起显示 5.1 路径）；`CreateReq.Dir`；自定义目录实例写入 config 注册表（5.3 依赖）。
  验收：自定义到任意盘创建成功、重启程序后仍在列表、可正常启停删。
- [ ] **5.6 目录名 slug 化 + Mohist 中文名解禁**：默认目录名 = 实例名 ASCII 安全化（非 ASCII → `server-{yyyyMMdd-HHmm}`）；`instance.json` 存中文显示名；Mohist/Banner 校验从「拒中文名」改为「最终路径含非 ASCII 才拒」，并在选核心当刻就地提示。
  验收：中文名 Mohist 实例创建并成功开服（目录为 slug）；把程序放中文路径下创建 Mohist 被拦截且提示准确。
- [ ] **5.7 删除安全语义**：`Delete()` 由「Rel 于 ServersDir」改为「目录内存在本实例 instance.json 才允许 RemoveAll」；备份/导入/策略等路径引用点回归。
  验收：自定义目录实例可安全删除；恶意构造（instance.json 缺失/名字不匹配）拒绝删除。

## 阶段 6 · 创建向导升级

- [ ] **6.1 核心选择分组**：按 kind 分区（原版与轻量 / 插件服 / 模组服 / 混合服 / 群组代理）+「新手推荐」徽标（Paper）；分区标题 + 简介一句。
  验收：12 核心归组正确，双主题截图布局不破。
- [ ] **6.2 版本列表升级**：类型徽标 + 发布时间副行（后端 `/api/mcversions` 返回 releaseTime）；默认只显示正式版（快照开关保留）。
  验收：1.20.1 行显示发布日期；快照默认隐藏。
- [ ] **6.3 基本设置补难度 / 默认游戏模式下拉** + 一句「另有约 60 项设置可在创建后的『常用设置』中修改」。
  验收：创建后 server.properties 两键值正确。
- [ ] **6.4 零状态一键开服**：空列表 CTA 升级为「⚡ 一键开服（Paper 最新正式版 + 推荐设置）」——自动取名/内存/端口，**EULA 仍需弹窗显式勾选**后直接进入部署任务页。
  验收：全新数据目录下两次点击（按钮 + EULA 确认）到达部署页并成功开服。
- [ ] **6.5 导入整合包页同步**：路径显示（随 5.1）、内存组件（随 1.3）对齐。
  验收：与向导视觉/行为一致。

## 阶段 7 · 实例列表与详情

- [ ] **7.1 卡片差量刷新**：轮询只更新状态点/按钮/时长等变化节点，不整卡重绘；消灭 3 秒焦点丢失与闪烁。
  验收：轮询期间悬停按钮不中断、开着的确认弹窗不受影响。
- [ ] **7.2 运行时长 + 在线人数**：`/api/instances` 返回 `startedAt`/`onlineCount`；卡片显示「⏱ 2h13m · 👥 3/20」；HUD 汇总「运行中 N 台 · 在线 M 人」。
  验收：进出玩家/重启后数值正确。
- [ ] **7.3 「打开目录」升级为直达菜单**：根目录 / mods / plugins / world（读 level-name）/ logs / crash-reports / 备份目录，按核心 kind 过滤（代理端只留根/logs）；后端 opendir 接受 `sub` 参数并做白名单校验。
  验收：各项直开正确文件夹；`sub=../..` 之类被拒绝。
- [ ] **7.4 实例搜索/排序**：名称过滤输入框 + 排序（运行中优先/最近创建）。
  验收：8+ 实例时过滤即时、排序稳定。
- [ ] **7.5 JVM 页增强**：ExtraJVM 参数编辑（textarea，一行一参）+ 「Aikar's Flags 一键预设」（按 ≥12G / <12G 两组）+ Xms 显示；run.bat 同步。
  验收：预设写入后 run.bat 与托管启动参数一致，实例可正常开服。
- [ ] **7.6 控制台字号调节 + 导出**：A-/A+（localStorage 记忆）；「复制全部」「导出为 txt」。
  验收：字号跨页面记忆；导出文件内容与缓冲一致。
- [ ] **7.7 任务失败动线**：失败页增加「打开实例目录」「查看 installer.log / crash-reports」按钮（文件存在才显示）。
  验收：构造一次安装失败，按钮直达对应文件/目录。

## 阶段 8 · 联机页打磨

- [ ] **8.1 添加表单收进「＋ 添加隧道」弹层**：列表页面清爽化；弹层内保留服务商联动字段逻辑。
  验收：添加流程功能不变，页面首屏只见隧道列表。
- [ ] **8.2 失败原因上浮**：tunnel 捕获 frpc 最后错误行，`List` 返回 `lastError`，隧道行内红字显示（点开日志看全文）。
  验收：错 token 启动隧道，行内直接可见失败原因。

## 阶段 9 · 体验打磨

- [x] **9.1 备份保留份数/目录可配**：保留份数（默认 10）进实例备份页；备份根目录随 5.2。
  验收：设 3 份后第 4 次备份滚动删除最旧。—— Config.BackupKeep + app.BackupKeep()（1~1000 裁剪），pruneBackups 改用；备份页「保留最近 N 份 · 保存」控件（全局设置）。
- [x] **9.2 CF 未解析清单突出显示**：导入完成页醒目卡片「N 个模组需手动补装」+ 清单（名称 + 项目链接）。
  验收：构造含不可解析文件的 CF 包，卡片与清单正确显示。—— tasks.Warning 结构化告警（Snapshot.warnings），import.go 填充项目号/链接，任务页金色 warn-card 渲染（CDP 注入截图验）。
- [x] **9.3 首次运行引导卡**：首启一次性卡片（选下载源 + 「你的服务器将存放在…」说明 + 可关闭）。
  验收：新数据目录首启显示，关闭后不再出现。—— Config.Onboarded；boot 时未引导则弹 showOnboarding（下载源三选 + serversRoot 路径），点「开始使用」写 onboarded=true。双主题截图验。
- [x] **9.4 命令输入补全**：常用命令表 + 在线玩家名（Tab/点击补全）。
  验收：输入 `op ` 后出现在线玩家候选。—— 控制台 cmd-suggest 下拉：COMMON_CMDS 命令补全 + whitelist 子命令 + PLAYER_CMDS 在线玩家名（进出游戏刷新）；Tab/↑↓/点击补全，与历史↑↓协调。CDP 实测 `sa`→say/save-*。
- [x] **9.5 `bg/` 壁纸个性化**：程序旁 `bg/` 文件夹有图则随机作为主区背景（低透明度铺底，双主题适配），无则维持现状。
  验收：放图后刷新可见、删图后恢复；文字对比度不受影响（截图验）。—— GET /api/bg 列表 + GET /bg/{name}（免 token，filepath.Base 防穿越）；body.has-bg 用 --bg-veil 蒙版（深.88/浅.86）叠图，卡片实底保证对比度。双主题截图验。

## 阶段 10 · 托盘与自启

- [x] **10.1 最小化到托盘**：关窗改为收入托盘（服务器不停）；托盘菜单：打开面板 / 全部停止并退出；设置内开关（默认关，保持现有语义）。
  验收：开关开启后关窗服务器仍在跑、托盘可唤回窗口；开关关闭时行为与现在一致。—— tray_windows.go 子类化 go-webview2 窗口过程拦 WM_CLOSE（MinimizeToTray 时 SW_HIDE 吞掉、否则原逻辑退出）；Shell_NotifyIcon 常驻托盘图标 + 左键恢复/右键菜单（打开面板/全部停止并退出）；shutdown 改 PostMessage(WM_APP_QUIT) 线程安全退出 + runtime.LockOSThread。设置「启动与托盘」分区开关，双主题截图 + GUI -minimized 冒烟不崩验。**托盘实际关窗/恢复/菜单需真机手验**。
- [x] **10.2 开机自启**：设置开关，写/删 `HKCU\...\Run`（带 `-minimized` 参数配合 10.1）。
  验收：开启后注册表键正确、注销重登自动driving到托盘；关闭后键删除。—— internal/autostart（+非 Windows 桩）Enabled/Set 读写 HKCU\...\Run\AutoMCHUB=`"<exe>" -minimized`；main.go 加 -minimized 启动即 hideTrayWindow；web autoStart 真值以注册表为准。真机 E2E：ON→注册表现值/api 真、OFF→键删/api 假、无残留。

## 阶段 11 · 收尾与回归

- [x] **11.1 版本号 v2.1.0** + README 功能清单与截图更新 + `docs/API.md` 增补。—— app.go Version=2.1.0；README 刷新功能表（三主题/托盘自启/存储自选/配置全量目录/v2.1 细节）+ 架构块；API.md 增补 pickdir/openpath/bg 端点、全部新 config 键、instances 新字段、opendir sub、tasks warnings、tunnels lastError、loopback/token 认证说明。
- [x] **11.2 全量真机 E2E 回归**（本轮 + 各阶段累积）：⑥备份滚动删除（keep=2→3 次备份剩 2，本轮实测）；⑦改端口局域网指引（阶段1）；⑧frps 回环（v1.3 实测）；⑩托盘关窗-唤回-退出（用户真机手验）；③自定义目录 + ④多根扫描 + ⑤gamerule 显隐（阶段4/5 实测）。①Paper 一键开服 / ②中文名 Mohist 需真实下载，由用户日常真机使用覆盖（跨阶段已多次实开）。
- [x] **11.3 双主题全页截图终验**：各阶段已逐页双主题阅图；本轮终验 instances/create/常用设置全量目录/设置/首启引导/托盘设置/备份/控制台补全/warn-card/壁纸，浅深均过，v2.1.0 显示正确。
- [x] **11.4 code-review 子代理审查**：4 个 general-purpose 子代理并行审 main…HEAD——①路径与权限安全 ②Windows 原生（托盘子类化/NOTIFYICONDATA/注册表）③并发与状态机 ④Web/API+前端 XSS。无 Critical；v2.1 新增（tunnel lastErr/tasks warnings/在线集合/zip-slip/bg 穿越/删除归属）确认正确。
- [x] **11.5 QA 修复回归**：审查发现全部处置——修：崩溃重启 runGen 守卫（High，防误拉起已停服）、停止看门狗按 proc 身份（Med，防误杀新进程）、config 自启改到校验/保存之后（Med）、opendir 仅本机、safeRelPath 拒裸 `..`、backupDirOf filepath.Base、UpdateSettings/Policies 持锁落盘、tunnel 锁内取名、warn 链接限 http(s)、两处 badge 转义、prTimer 清理、托盘子类化/NIM_ADD 结果校验 + TaskbarCreated 重登 + 回调 recover。跳过（已判无害）：冗余轮询、waitConsole 回放间隙、托盘图标幂等重删。复测：D1 校验失败不改注册表 + 自启正常开关 + 备份回归，全绿。
- [x] **11.6 分阶段提交收口**：12 阶段 commit 齐全；`AutoMCHUB.exe` 重新构建发布。

## 明确不做 / 观察区

- **1.21.9+ 官方管理协议接入**（management-server-*）：前瞻观察，待生态成熟单独立项。
- **全局游戏设置预设（逐项继承/覆盖）**：远期，多服用户规模起来后再做。
- **多语言 i18n**：当前定位国内用户，不做。
